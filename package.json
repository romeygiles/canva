{
  "name": "cards-and-invites",
  "version": "1.0.0",
  "private": true,
  "description": "Design printable greeting cards and party invitations in the browser.",
  "type": "module",
  "scripts": {
    "start": "npx --yes http-server . -p 8080 -c-1 -o",
    "test": "node tests/smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
